"""
starship — Isaac Sim Starship simulation modules.

Modules:
  create_stage       — USD stage generator (run once, offline)
  starship_publisher — reads physics state → publishes ROS 2 topics
  starship_controller — subscribes ROS 2 commands → applies PhysX forces
"""
