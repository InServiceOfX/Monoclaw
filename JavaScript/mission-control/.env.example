# Copy this file to .env and fill in your API keys.
# All keys are optional — only fill in keys for models you want to use.
# Keys without values will show the endpoint as unreachable.

# Anthropic (Claude Sonnet 4.6)
VITE_ANTHROPIC_API_KEY=sk-ant-...

# NVIDIA NIM (Kimi K2.5)
VITE_NVIDIA_NIM_API_KEY=nvapi-...

# xAI (Grok 4.1 Fast)
VITE_XAI_API_KEY=xai-...

# Groq (GPT-OSS 20B)
VITE_GROQ_API_KEY=gsk_...

# SGLang local (Qwen3-1.7B) requires no key — just run the server:
#   python -m sglang.launch_server --model Qwen/Qwen3-1.7B --port 30000
